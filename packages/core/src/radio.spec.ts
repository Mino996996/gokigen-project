describe('RadioScript', ()=>{
  it('create an RadioScript class', () =>{
    expect(()=> new RadioScript()).toBeDefined();
  });

  it('経過時間を表示する', () =>{
    const startTime =''; //date(~~~);
    // 予定時間が一致したらtrueを返すとかそんな感じ
  });

})
